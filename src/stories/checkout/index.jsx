import React, { PropTypes } from 'react'
import styles from './styles'

const Checkout = ({}) => (
    <div className="root">
        Hello world!

        { styles() }
    </div>
)

Checkout.propTypes = {

}

export default Checkout
